/**
 * Test setup — runs once before any test file.
 *
 * Pulls in jest-dom's extra matchers like `toBeInTheDocument()` and
 * `toHaveTextContent()` so component tests can make readable assertions
 * about what's rendered.
 */

import "@testing-library/jest-dom/vitest";
